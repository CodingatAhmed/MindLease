{
  "name": "@mindlease/shared",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests defined for shared package\"",
    "clean": "rm -rf dist && find src -name '*.js' -delete -o -name '*.d.ts' -delete -o -name '*.js.map' -delete"
  },
  "exports": {
    ".": "./index.ts"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}